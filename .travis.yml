language: python

python:
  - 3.3

virtualenv:
  system_site_packages: true

notifications:
  email: false

before_install:
- sudo apt-get install -qq python3-dev libatlas-base-dev gfortran

install:
  - pip install numpy==1.7
  - pip install -r requirements.txt --use-mirrors

script: nosetests
